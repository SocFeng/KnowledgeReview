# ===== LangChain / LangGraph 核心 =====
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-community>=0.3.0,<0.4.0
langgraph>=0.2.40,<0.3.0
langgraph-checkpoint>=2.0.0
langgraph-checkpoint-sqlite>=2.0.0

# ===== LLM Provider：阿里云 DashScope（Qwen） =====
# 默认走 DashScope 的 OpenAI 兼容端点 + ChatOpenAI
#   （stream + tool_calls 更稳定，避开 ChatTongyi _stream 的已知 bug）
# ChatTongyi + dashscope SDK 保留作为 fallback
langchain-openai>=0.2.0,<0.4.0
dashscope>=1.20.0

# ===== HTTP 工具调用 =====
requests>=2.31.0
httpx>=0.27.0

# ===== Web UI =====
streamlit>=1.38.0

# ===== 配置 / 数据 =====
python-dotenv>=1.0.1
pydantic>=2.7.0
pydantic-settings>=2.4.0

# ===== 可选：地理 / 数学 =====
# 用于 haversine 距离计算（也可纯手写，避免依赖）
geopy>=2.4.1
